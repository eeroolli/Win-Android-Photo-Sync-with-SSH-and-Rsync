# Personal configuration files (contain private data)
copy_config.conf

# Log files (contain personal data and timestamps)
sync_log*.txt
device_sync_log*.csv
delete_copied_summary*.txt
copied_device_files.log

# Hash databases (contain file fingerprints)
device_copied_hashes.txt
imported_to_lightroom_hashes.txt
imported_to_lightroom_hashes.csv

# Temporary files created during script execution
*_hashes_only.txt
already_imported_hashes.txt
src_hashes_only.txt
files_to_delete.txt

# Backup files
*.backup
*.bak

# Old script versions
sync_android_photos(*).sh
# If you want to ignore all old numbered/backup scripts, uncomment the next line:
# *(*).sh

# OS generated files
.DS_Store
Thumbs.db

# Legacy/obsolete log and hash files (no longer in use)
imported_device_files.log
import_config.conf
sync_config.conf
device_imported_hashes.txt
delete_imported_summary*.txt
kopiert_files.txt
archive/
old_logs/
# End legacy/obsolete
